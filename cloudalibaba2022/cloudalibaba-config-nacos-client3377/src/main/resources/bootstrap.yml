server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #服务注册中心地址
        server-addr: 139.155.39.21:8848
      config:
        #server-addr: localhost:8848 #配置中心地址
        server-addr: 139.155.39.21:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置（yml和yaml都可以）
        #默认组的dev环境配置获取
        group: DEFAULT_GROUP
        namespace: ea51389d-02f9-4692-b132-65535bccb64b

#${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
#nacos-config-client-dev.yaml  (一定要与file-extension值保持一致)
