spring:
  profiles:
    # prod, dev
    active: dev
  application:
    name: emall-study-ecommerce

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
